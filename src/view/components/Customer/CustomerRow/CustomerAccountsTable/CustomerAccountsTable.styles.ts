import { SxProps, Theme } from '@mui/material/styles';

export const customerAccountsTableStyles = {
    paper: {
        mb: 3,
    } as SxProps<Theme>,

    tableRow: {
        cursor: 'pointer',
    } as SxProps<Theme>,

    noDataCell: {
        textAlign: 'center',
    } as SxProps<Theme>,
};