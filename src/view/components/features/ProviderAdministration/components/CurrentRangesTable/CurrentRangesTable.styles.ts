import { SxProps, Theme } from '@mui/material';

export const styles = {
    paper: {
        p: 2
    } as SxProps<Theme>,

    subtitle1: {
        mb: 2,
        fontWeight: 600
    } as SxProps<Theme>,

    providerSubtitle: {
        mb: 2,
        fontWeight: 500,
        color: 'text.secondary'
    } as SxProps<Theme>
};