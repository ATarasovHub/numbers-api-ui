import { SxProps, Theme } from '@mui/material/styles';

export const customerCountriesTableStyles = {
    paper: {

    } as SxProps<Theme>,

    noDataCell: {
        textAlign: 'center',
    } as SxProps<Theme>,
};